{
  "name": "My EDPS Container",
  "workspaceFolder": "/home/user",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.11",
      "installJupyterlab": true
    }
  },
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "echo 'Ready!'",
  "postStartCommand": "panel serve edps-gui.py --plugins pdf_handler --address 0.0.0.0 --port 7860 --allow-websocket-origin='*' >& /home/user/panel.log & repo_name=$(basename -s .git `git config --get remote.origin.url`); echo Run the GUI at: https://${CODESPACE_NAME}-7860.${GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN}/${repo_name}",
  "forwardPorts": [7860],
  "portsAttributes": {
    "7860": {
      "label": "EDPS App",
      "visibility": "public"
    }
  }
}
